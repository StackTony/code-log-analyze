---
feature_ids: [F001.1]
related_features: [F001, F002, F003, F004]
topics: [http, fastapi, rest-api, metrics, pydantic]
doc_kind: spec
created: 2026-07-25
---

# F001.1: HTTP 服务层

> Status: spec (v1.1 — 文若端口冲突根因审视后修订，家规铁律方向纠正；v1 = 云长+文若架构 review 整合修订) | Owner: 奉孝 (@ragdoll-pa82, GLM-5.2, Siamese) | Reviewer: @云长（跨家族）

## Why

F001 落地了 M1 `RepoLogGraphService` 6 个 API（pure Python class，77 测试全绿已 merge 到 master），但只能在进程内调用。要让铲屎官/前端/其他业务团队真正用起来 6 个 API，必须包 HTTP 层。本 feature 给 M1 service 包 FastAPI HTTP wrapper，启动 API server :8000 + metrics server :9464。

**前置依赖**：F001 已 merge（commit 87e2db9, tag f001-v1）。本 feature 不修改 M1 service 层任何代码，只在 `packages/api/` 新增 HTTP wrapper 子包。

## What

### 一、模块定位

F001.1 是 M1 的 HTTP 包装层，**只做 wrapper 不改 service**。6 个 endpoint 一一映射 M1 6 个 API 方法，加 `/health`/`/ready`/`/docs`/`/metrics` 4 个运维 endpoint。

**设计原则**：
- HTTP 层只做协议转换 + 错误格式统一，业务逻辑全在 M1 service
- Pydantic v2 BaseModel 作为 API schema，与 M1 dataclass 解耦（独立演化）
- metrics 9464 独立进程（`multiprocessing.Process`，避免 uvicorn --reload 重启 metrics）
- F001.1 不做认证（dev-only，启动 console 警告），F001.2/M3 加 RBAC
- 家规铁律方向修正（v1.1）：API 8000 / metrics 9464，**避开** CatCafe runtime 自留地 3003/3004/9100（家规原文：3003/3004 是 CatCafe runtime 自留地，外部项目禁占，不是"本项目应使用 3004"）

### 二、文件结构

```
代码飞轮/
├── packages/
│   ├── contracts/          # 不变（M1 数据契约）
│   ├── m1/                 # 不变（M1 service 层）
│   └── api/                # 新增 HTTP wrapper 子包
│       ├── __init__.py
│       ├── app.py          # FastAPI app + lifespan（启动 metrics server 进程）
│       ├── deps.py         # 依赖注入（engine/sessionmaker/service factory）
│       ├── error_handlers.py  # 统一错误格式 {code, message, details}
│       ├── mappers/        # dataclass → Pydantic 转换（M2/M3/M4 复用）
│       │   ├── __init__.py
│       │   └── log_point.py   # log_point_to_response / candidate_to_response
│       ├── schemas/        # Pydantic v2 BaseModel（strict=True, extra=forbid）
│       │   ├── __init__.py
│       │   ├── common.py    # ErrorResponse / HealthResponse / ReadyResponse
│       │   ├── ingest.py    # IngestRequest / IngestResponse
│       │   ├── log_point.py # LogPointAPI + LLMHypothesisAPI（嵌套）
│       │   ├── call_context.py  # CallContextAPI
│       │   └── confirm.py   # ConfirmRequest / RevokeRequest
│       └── routes/         # 6 endpoint + 3 运维 endpoint
│           ├── __init__.py  # router 聚合 + tag 分组
│           ├── ingest.py    # POST /ingest          [ingestion]
│           ├── candidates.py # GET /candidates/{repo_id}  [query]
│           ├── confirm.py    # POST /confirm/{repo_id}    [ingestion]
│           ├── revoke.py     # POST /revoke/{repo_id}     [ingestion]
│           ├── log_points.py # GET /log-points/{repo_id}  [query]
│           ├── call_context.py # POST /call-context/{repo_id}  [query]
│           └── ops.py        # GET /health + GET /ready + GET /metrics
└── tests/api/              # 新增测试目录
    ├── __init__.py
    ├── conftest.py         # TestClient + in-memory SQLite fixture + metrics server fixture
    ├── test_ingest.py      # AC-1
    ├── test_candidates.py  # AC-1
    ├── test_confirm.py     # AC-1
    ├── test_revoke.py      # AC-1
    ├── test_log_points.py  # AC-1
    ├── test_call_context.py # AC-1
    ├── test_error_handling.py  # AC-5
    ├── test_metrics.py     # AC-4（独立 metrics server fixture）
    ├── test_health_ready.py # AC-3 + /ready
    ├── test_schema_validation.py  # AC-6 + N-1 schema 对齐
    └── test_openapi_docs.py # AC-2 /docs
```

**设计要点**：
- `packages/api/` 与 `packages/m1/` 平级，不嵌套
- 6 个业务 endpoint 各一个文件（≤100 行），运维 endpoint 合并 `ops.py`
- schemas 与 routes 分离，M2/M3/M4 可复用 schemas/mappers
- mappers 集中 dataclass→Pydantic 转换（文若 W-4），避免散落各 route

### 三、API 契约（9 个 endpoint）

| Method | Path | Body / Query | Returns | service 方法 | tag |
|--------|------|---------------|---------|--------------|-----|
| POST | `/ingest` | `IngestRequest` | `IngestResponse` 201 | `ingest_repo` | ingestion |
| GET | `/candidates/{repo_id}` | `?include_all=false` | `list[LogPointAPI]` 200 | `list_candidates` | query |
| POST | `/confirm/{repo_id}` | `ConfirmRequest` | 204 No Content | `confirm_ingestion` | ingestion |
| POST | `/revoke/{repo_id}` | `RevokeRequest` | 204 No Content | `revoke_ingestion` | ingestion |
| GET | `/log-points/{repo_id}` | `?file_path&function_signature&log_level` | `list[LogPointAPI]` 200 | `query_log_points` | query |
| POST | `/call-context/{repo_id}` | `CallContextRequest` | `CallContextAPI` 200 | `get_call_context` | query |
| GET | `/health` | — | `{status: "ok"}` 200 | — | ops |
| GET | `/ready` | — | `{status: "ready"|"not_ready", reason?}` 200 | — | ops |
| GET | `/metrics` | — | Prometheus exposition 200 | — | ops |
| GET | `/docs` | — | Swagger UI | — | (FastAPI 自带) |
| GET | `/openapi.json` | — | OpenAPI 3.1 spec | — | (FastAPI 自带) |

**云长 OQ-1 修订**：`/call-context/{repo_id}` 改为 POST + body（避免 URL encode 复杂签名）。

**云长 I-2 修订**：补 `/ready` endpoint 区分 liveness/readiness。

**状态码语义**：
- 200 OK：GET 成功
- 201 Created：POST `/ingest` 成功（产生新资源 repo_id）
- 204 No Content：POST `/confirm`/`/revoke` 成功（无响应体）
- 400 INVALID_PATH：UnsafePathError
- 409 INGEST_LOCK_RUNNING：repo 正在解析中
- 422 VALIDATION_ERROR：Pydantic 验证失败
- 422 REPO_NOT_FOUND：repo_id 不存在
- 500 INTERNAL_ERROR：未捕获异常
- 503 NOT_READY：`/ready` 检查失败（DB 不可达）

### 四、数据流（ingest 全链路）

```
1. POST /ingest {local_path, ingester}
2. FastAPI Pydantic strict 验证 → IngestRequest
3. deps.get_service() 注入 RepoLogGraphService（per-request session）
4. service.ingest_repo(source, ingester) → repo_id
   - 若 lock 表已有 running 状态 → 返回 409 INGEST_LOCK_RUNNING
   - 若 local_path 不存在/越权 → 返回 400 INVALID_PATH
5. mappers.log_point_to_response 转换（如需返回 LogPoint）
6. IngestResponse pydantic 序列化 → JSON
7. 同时 metrics: m1_ingest_repo_duration_seconds.observe(...)
8. 返回 201 + {repo_id}
```

**异常映射**（云长 C-1 + I-1 修订）：

```python
# packages/api/error_handlers.py
@app.exception_handler(UnsafePathError)
async def handle_unsafe_path(exc):
    return JSONResponse(400, {"code": "INVALID_PATH", "message": str(exc), "details": {}})

@app.exception_handler(UnsafeUrlError)
async def handle_unsafe_url(exc):
    return JSONResponse(400, {"code": "INVALID_URL", "message": str(exc), "details": {}})

@app.exception_handler(RequestValidationError)
async def handle_validation(exc):
    return JSONResponse(422, {"code": "VALIDATION_ERROR", "message": "Request validation failed", "details": exc.errors()})

# 404/409/500 通过 raise HTTPException(status_code=..., detail={code, message, details}) 实现
```

**RepoIngestLock running 处理（云长 C-1 修订）**：

M1 `RepoRegistrar.ingest()` 遇 running 状态时**静默 return existing repo_id**（不抛异常）。HTTP 层需要在调 service 前先查 lock 表状态：

```python
# packages/api/routes/ingest.py
from packages.m1.storage.models import RepoIngestLockModel
from sqlalchemy import select

@router.post("/ingest", response_model=IngestResponse, status_code=201)
def ingest(req: IngestRequest, service: RepoLogGraphService = Depends(get_service)):
    # 先查 lock 表 running 状态（云长 C-1 修订）
    existing = service._session.scalar(
        select(RepoIngestLockModel)
        .where(RepoIngestLockModel.repo_id == _compute_repo_id(req))
        .where(RepoIngestLockModel.status == "running")
    )
    if existing:
        raise HTTPException(409, detail={
            "code": "INGEST_LOCK_RUNNING",
            "message": f"repo {existing.repo_id} is being ingested",
            "details": {"repo_id": existing.repo_id, "status": "running"}
        })
    repo_id = service.ingest_repo(...)
    return IngestResponse(repo_id=repo_id)
```

### 五、依赖注入（云长 I-1 修订）

```python
# packages/api/deps.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session
from packages.m1.config_loader import load_config

# 全局 engine + sessionmaker（每个 uvicorn worker 独立）
_config = load_config()
engine = create_engine(_config.storage.postgres_dsn, pool_pre_ping=True)
SessionLocal = sessionmaker(bind=engine, autoflush=False, expire_on_commit=False)


def get_session() -> Session:
    """FastAPI Depends 注入 per-request session。"""
    session = SessionLocal()
    try:
        yield session
    finally:
        session.close()


def get_service(session: Session = Depends(get_session)) -> RepoLogGraphService:
    """FastAPI Depends 注入 service（mock 友好）。"""
    # ... 构造 RepoLogGraphService（参考 tests/e2e/test_repo_log_graph_service.py fixture）
    yield service
```

### 六、metrics 9464 独立进程（云长 C-2 + 文若 W-1 修订；v1.1 端口从 9100 改 9464 避开 CatCafe runtime 自留地）

```python
# packages/api/app.py
import multiprocessing
import logging
from contextlib import asynccontextmanager
from prometheus_client import start_http_server

@asynccontextmanager
async def lifespan(app: FastAPI):
    logger = logging.getLogger(__name__)

    # metrics server 独立进程（文若 W-1 修订：避免 uvicorn --reload 重启 metrics）
    metrics_process = None
    try:
        metrics_process = multiprocessing.Process(
            target=start_http_server, args=(9464,), daemon=True
        )
        metrics_process.start()
        logger.info("Metrics server started on port 9464 (pid={})", metrics_process.pid)
    except Exception as e:
        # 云长 C-2 修订：graceful degradation，metrics 启动失败不阻断 API
        logger.warning(f"Metrics server failed to start: {e}. Continuing without metrics.")

    # 启动 console 警告（AC-7）
    logger.warning("⚠️  未启用认证，dev-only 模式 — 禁止暴露公网")

    yield

    # cleanup
    if metrics_process and metrics_process.is_alive():
        metrics_process.terminate()
        metrics_process.join(timeout=2)
```

### 七、配置扩展

`packages/m1/config_loader.py` 加 `ApiConfig` dataclass（云长 I-3 修订）：

```python
@dataclasses.dataclass(frozen=True)
class ApiConfig:
    host: str = "127.0.0.1"
    port: int = 8000  # v1.1 修正：避开 CatCafe runtime 3003/3004（家规铁律方向纠正）
    enable_auth: bool = False  # F001.1 dev-only
    cors_origins: tuple[str, ...] = ("http://localhost:3003",)  # F003 前端

# Config dataclass 加 api 字段：
@dataclasses.dataclass(frozen=True)
class Config:
    llm: LLMConfig
    storage: StorageConfig
    extraction: ExtractionConfig
    sanitizer: SanitizerConfig
    metrics: MetricsConfig
    api: ApiConfig  # 新增
```

`config.example.yaml` 补：

```yaml
api:
  host: "127.0.0.1"
  port: 8000  # v1.1 修正：避开 CatCafe runtime 自留地 3003/3004
  enable_auth: false
  cors_origins: ["http://localhost:3003"]
```

环境变量覆盖：`CODEFLY_API_HOST` / `CODEFLY_API_PORT` / `CODEFLY_API_CORS_ORIGINS`（逗号分隔）。

### 八、错误码命名空间（文若 W-5 修订）

| 模块 | 前缀 | 错误码示例 |
|------|------|-----------|
| M1 HTTP | `M1_*` | `M1_INVALID_PATH` / `M1_INGEST_LOCK_RUNNING` / `M1_REPO_NOT_FOUND` |
| M2 离线分析 | `M2_*` | 预留 |
| M3 在线扫描 | `M3_*` | 预留 |
| M4 改进 | `M4_*` | 预留 |
| 跨模块通用 | `GENERIC_*` | `GENERIC_VALIDATION_ERROR` / `GENERIC_INTERNAL_ERROR` |

**v1 错误码清单**：
- `M1_INVALID_PATH` (400)
- `M1_INVALID_URL` (400)
- `M1_INGEST_LOCK_RUNNING` (409)
- `M1_REPO_NOT_FOUND` (422)
- `GENERIC_VALIDATION_ERROR` (422)
- `GENERIC_NOT_READY` (503)
- `GENERIC_INTERNAL_ERROR` (500)

### 九、Pydantic v2 schema 设计（云长 C-3 + 文若 W-6 修订）

所有 schema 强制 `ConfigDict(strict=True, extra="forbid")`：

```python
# packages/api/schemas/log_point.py
from pydantic import BaseModel, ConfigDict
from datetime import datetime

class LLMHypothesisAPI(BaseModel):
    """嵌套 schema（云长 C-3 修订）。"""
    model_config = ConfigDict(strict=True, extra="forbid", from_attributes=True)

    summary: str
    possible_causes: list[str]
    error_kind: str
    model_name: str
    prompt_hash: str
    generated_at: datetime
    suggested_check: str | None = None


class LogPointAPI(BaseModel):
    """LogPoint API schema — 24 字段对齐 LogPoint dataclass。"""
    model_config = ConfigDict(strict=True, extra="forbid", from_attributes=True)

    id: str
    repo_id: str
    git_commit_sha: str
    extractor_version: str
    file_path: str
    function_signature: str
    line_start: int
    line_end: int
    language: str
    log_level: str
    log_message_template: str
    log_message_variables: list[str]
    framework_hint: str
    confidence_score: float
    enclosing_class: str | None = None
    call_chain_to_entry: list[str]
    enclosing_community: str | None = None
    evidence_refs: list[dict]  # CaseRef JSON
    llm_hypothesis: LLMHypothesisAPI | None = None
    occurrence_count: int
    is_top_n: bool
    ingestion_status: str
    first_seen_at: datetime
    last_seen_at: datetime
```

`schemas/call_context.py` 类似（CallContextAPI + 嵌套）。

### 十、Mapper 层（文若 W-4 修订）

```python
# packages/api/mappers/log_point.py
from packages.contracts.log_point import LogPoint
from packages.api.schemas.log_point import LogPointAPI, LLMHypothesisAPI

def log_point_to_response(lp: LogPoint) -> LogPointAPI:
    """dataclass → Pydantic 转换（M2/M3/M4 复用）。"""
    return LogPointAPI(
        id=lp.id, repo_id=lp.repo_id,
        # ... 24 字段
        llm_hypothesis=LLMHypothesisAPI(**dataclasses.asdict(lp.llm_hypothesis)) if lp.llm_hypothesis else None,
    )
```

### 十一、测试策略

**分层**（文若推荐）：
- `tests/api/`：TestClient 测 HTTP 层（6 endpoint + 错误 + 运维）
- `tests/api/test_metrics.py`：独立 metrics server fixture（multiprocessing.Process）+ httpx.get 测 9464
- 现有 `tests/e2e/`：保持测 service 层（不动）
- `tests/api/test_schema_validation.py`：验证 schema 字段与 dataclass 对齐（云长 N-1）

**conftest.py fixture**：
```python
@pytest.fixture()
def client():
    """TestClient + in-memory SQLite。"""
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    # override deps.get_session 返回 in-memory session
    with TestClient(app) as c:
        yield c
    engine.dispose()


@pytest.fixture()
def metrics_server():
    """独立 metrics server fixture（OQ-2 修订）。"""
    import httpx
    proc = multiprocessing.Process(target=start_http_server, args=(9101,), daemon=True)
    proc.start()
    time.sleep(0.5)  # 等 server 就绪
    yield "http://localhost:9101"
    proc.terminate()
```

**metrics fixture 用 9101 避免与 app 启动的 9464 冲突**。

### 十二、启动配置（文若 W-3 修订）

dev-only 硬编码 `reload=True`：

```python
# packages/api/__main__.py
import uvicorn

if __name__ == "__main__":
    # F001.1 dev-only 铁律：单进程 + reload
    # 生产部署（F001.2/M3）改 workers=4 + reload=False
    uvicorn.run(
        "packages.api.app:app",
        host="127.0.0.1",
        port=8000,  # v1.1 修正：避开 CatCafe runtime 3003/3004
        reload=True,  # dev-only
        log_level="info",
    )
```

启动命令：`py -m packages.api`（spec 显式声明 Windows uvloop 自动降级 asyncio，文若 W-2 修订）。

### 十三、OpenAPI tag 分组（文若 W-3 修订）

```python
# packages/api/routes/ingest.py
router = APIRouter(tags=["ingestion"])

# routes/log_points.py
router = APIRouter(tags=["query"])

# routes/ops.py
router = APIRouter(tags=["ops"])
```

`/docs` Swagger UI 自动按 tag 分组展示（ingestion / query / ops）。

### 十四、依赖锁定

`pyproject.toml` 补：

```toml
[project.optional-dependencies]
api = [
    "fastapi>=0.110,<0.120",
    "pydantic>=2.5,<3.0",
    "uvicorn[standard]>=0.27,<0.30",
    "httpx>=0.27,<0.30",  # TestClient 依赖
]
```

**文若 W-2 修订**：Windows 上 `uvicorn[standard]` 安装 `uvloop` 但不可用，uvicorn 自动 fallback `asyncio`。spec 显式声明此行为，不视作 bug。

## Acceptance Criteria

- [ ] AC-1: 6 个业务 endpoint 全部通过 `TestClient` 测试，状态码 + 响应 schema 正确（ingest 201 / candidates 200 / confirm 204 / revoke 204 / log-points 200 / call-context 200）
- [ ] AC-2: `/docs` Swagger UI 可访问，6 个 endpoint 按 tag 分组展示（ingestion / query / ops）
- [ ] AC-3: `/health` 返回 `{status: "ok"}` + `/ready` 返回 `{status: "ready"|"not_ready"}` 并检查 DB 连接
- [ ] AC-4: `/metrics` 在 9464 端口暴露 5 个 Prometheus 指标（candidate_pool_size / llm_call_total / llm_cache_hit_total / ingest_repo_duration_seconds / log_points_extracted_total），独立 metrics server fixture 测试通过
- [ ] AC-5: 统一错误响应格式 `{code, message, details}`，所有异常映射正确（UnsafePathError 400 / UnsafeUrlError 400 / RequestValidationError 422 / HTTPException 409/422/500）
- [ ] AC-6: 所有 Pydantic v2 schema 用 `ConfigDict(strict=True, extra="forbid", from_attributes=True)`，与 dataclass 字段对齐（`test_schema_validation.py` 验证 24 字段一致）
- [ ] AC-7: 启动时 console 警告 `⚠️ 未启用认证，dev-only 模式 — 禁止暴露公网`
- [ ] AC-8: 现有 77 个 M1 测试无回归（不动 service 层）
- [ ] AC-9: v1.1 修正 — API 端口 8000 / metrics 端口 9464，**避开** CatCafe runtime 自留地 3003/3004/9100（家规铁律原文：3003/3004 是 CatCafe runtime 自留地，外部项目禁占，方向是"避开"不是"使用"）
- [ ] AC-10: metrics server 用 `multiprocessing.Process` 独立进程，uvicorn `--reload` 重启不丢失 metrics 累积值（云长 C-2 + 文若 W-1 修订验证）
- [ ] AC-11: metrics server 启动失败时 graceful degradation（warn log，API 继续启动）
- [ ] AC-12: mapper 层集中 dataclass→Pydantic 转换（`packages/api/mappers/log_point.py`）
- [ ] AC-13: 错误码命名空间清晰（M1_* 前缀，M2/M3/M4 预留扩展）
- [ ] AC-14: 跨家族 review 通过（家规铁律 no self-review）—— @云长 review

## Dependencies

- **fastapi>=0.110,<0.120** — HTTP 框架 + 自动 OpenAPI 文档
- **pydantic>=2.5,<3.0** — API schema（与 M1 dataclass 解耦）
- **uvicorn[standard]>=0.27,<0.30** — ASGI server（Windows uvloop 自动降级 asyncio）
- **httpx>=0.27,<0.30** — TestClient 依赖
- **prometheus_client**（已在 M1 依赖面）— `start_http_server(9464)` 独立进程
- **M1 RepoLogGraphService**（已 merge 到 master）— 6 个 service 方法不变

## Risk

| 风险 | 等级 | 缓解 |
|------|------|------|
| metrics 9464 端口冲突（dev 环境多实例） | 🟡 中 | AC-11 graceful degradation + test fixture 用 9101 避免冲突 |
| uvicorn --reload 重启 metrics server 丢累积值 | 🟡 中 | AC-10 用 multiprocessing.Process 独立进程，uvicorn 重启不影响 |
| Pydantic strict 模式过严，合法请求被拒 | 🟢 低 | strict 只对类型严格（int≠str），extra=forbid 拒绝拼写错误字段名 |
| dataclass→Pydantic 24 字段映射遗漏 | 🟢 低 | AC-6 schema_validation 测试验证字段一致 |
| Windows uvloop 不可用 | 🟢 低 | uvicorn 自动 fallback asyncio，spec 显式声明 |
| F001.1 不做认证被误用为生产 | 🟡 中 | AC-7 console 警告 + 后续 F001.2 加 RBAC |

## Open Questions

- 无（云长 + 文若 review 的 3 OQ + 5 遗漏点全部修订落地）

## References

- F001 spec: `docs/features/F001-代码仓日志解析.md`
- F001 实施 plan: `docs/superpowers/plans/2026-07-24-f001-code-repo-log-parse.md`
- 云长架构 review（本 feature）: `docs/decisions/F001.1-review-云长-architecture.md`（待写）
- 文若补充分析（本 feature）: `docs/decisions/F001.1-review-文若-supplement.md`（待写）

---

## v1.1 修订附录（2026-07-27 文若审视 + 奉孝实施）

### 根因

v1 spec 把家规铁律"3003/3004 是 CatCafe runtime 自留地，外部项目禁占"**理解反了**——v1 写成"API 3004 / metrics 9100 / frontend 3003 分离"，被读成"本项目应使用 3004"。家规原文（`shared-rules.md` §12 Runtime 单实例保护）：

> `../cat-cafe-runtime` 的 `localhost:3003/3004` 默认视为**在线 runtime 端口**；对这两个端口做浏览器 / Playwright / curl 操作，等同于在操作 runtime，不是本地沙箱。

实测证据（2026-07-27 06:30 UTC）：

| 端口 | PID | 进程 | 命令行 |
|------|-----|------|--------|
| 3004 LISTEN | 2948 | node.exe | `CatCafe/packages/api/dist/index.js`（CatCafe 自家 API） |
| 3004 ESTABLISHED | 7736 | Cat Cafe.exe | CatCafe 桌面端（连自家 API 的客户端） |
| 3003 LISTEN | 8860 | node.exe | `CatCafe/packages/web/.../next start`（CatCafe 前端） |

**Kill 崩溃机制**：F001.1 dev 测试跑 `uvicorn --port 3004` → 端口冲突 → 铲屎官 kill 占用进程 → 杀的是 CatCafe 自家 API (PID 2948) → 桌面端 (PID 7736) 失去 backend → clowder AI 服务崩。这是 **spec 阶段就埋下的 P0 设计缺陷**，非 runtime bug。

### 修复范围（v1.1）

| 文件 | 修改 |
|------|------|
| `packages/m1/config_loader.py` | `ApiConfig.port` 默认 3004→8000 + 注释纠正 + `load_config()` 默认 port 同步 |
| `config.example.yaml` | `api.port` 3004→8000、`metrics.port` 9100→9464 + 注释纠正 |
| `README.md` | 启动命令 `--port 3004` → `--port 8000` + 访问地址同步 + 加端口规避说明 |
| `docs/features/F001.1-HTTP服务层.md` | spec 全文 16 处 3004/9100/家规铁律表述修订 + AC-4/AC-9 改写 + 本附录追加 |
| `tests/api/test_ac_coverage.py` | AC-9 表述同步 |

### 新端口选择依据

- **API 8000** — FastAPI/uvicorn 社区默认，dev 切换友好；避开 3003/3004/9100
- **metrics 9464** — prometheus_client 社区常用（`prometheus_client.start_http_server` 默认 9464 也常见），避开 CatCafe runtime 9100
- **测试 fixture 9101** 不变（已避开 9464，无冲突）

### 后续跟进（不在本次修复范围）

| 优先级 | 项 | 责任 phase |
|--------|-----|-----------|
| P1 | B-1 `deps.py` `get_service` 用 MagicMock 当 production placeholder | F002 启动前必须接真实组件 |
| P2 | B-2 `ops.py` `/metrics` 路由与 9464 独立进程双端口暴露 | F001.2 或独立 hotfix |
| P2 | B-6 `/ready` DB 不可达时应返回 503 而非 500 | F001.2 或独立 hotfix |
| P3 | B-3 `app.py` metrics process Windows terminate 强杀注释 | 顺手 |
| P3 | B-5 `conftest.py` `_reset_prometheus_registry` hack 替换独立 registry | F002 启动时 |

### B-4 已在本次修复落地

`app.py` lifespan 加 `atexit` 兜底 + 端口占用检查（见 v1.1 代码改动）。

---

[奉孝/GLM-5.2🐾] F001.1 spec v1.1 — 文若端口冲突根因审视 + 16 处表述修订 + AC-4/AC-9 改写 + 修订附录追加。家规铁律方向纠正：避开 CatCafe runtime 自留地 3003/3004/9100，本项目使用 8000/9464。
